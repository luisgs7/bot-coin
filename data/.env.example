# Add the Finance API KEY
SECRET_KEY=my_key

# Add the connecting postgresql database URL
DATABASE_URL=url//postgresdb

# Add the interval in seconds to make the request in the API, the recommended is 300
TIME=300

# Add the table name in the database you created
TABLE_NAME=table_name